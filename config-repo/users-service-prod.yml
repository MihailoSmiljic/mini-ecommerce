spring:
  jpa:
    show-sql: false
  h2:
    console:
      enabled: false

logging:
  level:
    com.ecommerce: WARN
    root: WARN

info:
  environment: "PRODUCTION - minimalni logovi, H2 konzola isključena"